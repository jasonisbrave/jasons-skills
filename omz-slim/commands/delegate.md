---
description: Delegate a task to a background omz-slim subagent (explorer | fixer)
---

Delegate the following task to a background subagent and report only its
summary when it finishes. Pick the agent per the omz-slim cheat-sheet
(explorer for read-only search/mapping, fixer for mechanical implementation).
Do not do the work yourself unless the task is trivial.

Task: $ARGUMENTS
